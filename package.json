{
  "name": "@masloff/paragonproto",
  "version": "0.1.6",
  "description": "Binary protocol for structured data with encryption support for every Paragon products",
  "module": "src/bun/index.ts",
  "type": "module",
  "files": [
    "src/bun/**/*",
    "src/bun/native/**/*"
  ],
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build:native": "bun scripts/build-native.ts",
    "build": "bun run build:native",
    "test:bun": "bun run src/bun/example.ts",
    "test": "cargo test --features std && bun run build:native && bun run src/bun/example.ts"
  }
}
