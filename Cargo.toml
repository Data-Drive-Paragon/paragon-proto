[package]
name = "paragon_proto"
version = "0.1.0"
edition = "2021"

[lib]
name = "paragon_proto"
crate-type = ["cdylib", "rlib"]

[profile.release]
panic = "abort"
lto = true
opt-level = "z"  
strip = true
codegen-units = 1

[profile.dev]
panic = "abort"


[dependencies]
